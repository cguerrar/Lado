@{
    ViewData["Title"] = "Acceso Denegado";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    .acceso-denegado {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 60vh;
        text-align: center;
        padding: 40px 20px;
    }

    .acceso-denegado i {
        font-size: 80px;
        color: #ef4444;
        margin-bottom: 24px;
    }

    .acceso-denegado h1 {
        font-size: 32px;
        color: var(--fg);
        margin-bottom: 16px;
    }

    .acceso-denegado p {
        font-size: 16px;
        color: var(--muted);
        max-width: 500px;
        margin-bottom: 32px;
    }

    .btn-volver {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 12px 24px;
        background: var(--primary);
        color: white;
        text-decoration: none;
        border-radius: 8px;
        font-weight: 500;
        transition: all 0.2s;
    }

    .btn-volver:hover {
        background: var(--primary-dark);
        transform: translateY(-2px);
    }
</style>

<div class="acceso-denegado">
    <i class="fas fa-lock"></i>
    <h1>Acceso Denegado</h1>
    <p>No tienes los permisos necesarios para acceder a esta seccion. Si crees que esto es un error, contacta con un administrador.</p>
    <a href="@Url.Action("Index", "Supervisor")" class="btn-volver">
        <i class="fas fa-arrow-left"></i>
        Volver al panel
    </a>
</div>
