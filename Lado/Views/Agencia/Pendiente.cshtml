@model Lado.Models.Agencia
@{
    ViewData["Title"] = "Solicitud Pendiente";
    Layout = "_Layout";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow-sm border-0 text-center" style="border-radius: 20px;">
                <div class="card-body p-5">
                    <div class="d-inline-flex align-items-center justify-content-center rounded-circle mb-4"
                         style="width: 100px; height: 100px; background: rgba(245, 158, 11, 0.1);">
                        <i class="fas fa-clock text-warning" style="font-size: 48px;"></i>
                    </div>

                    <h2 class="fw-bold mb-3">Solicitud en Revision</h2>
                    <p class="text-muted mb-4">
                        Tu solicitud para registrar la agencia <strong>@Model.NombreEmpresa</strong> esta siendo revisada por nuestro equipo.
                    </p>

                    <div class="bg-light rounded-3 p-4 mb-4">
                        <div class="row text-start">
                            <div class="col-6 mb-3">
                                <small class="text-muted d-block">Fecha de solicitud</small>
                                <span class="fw-medium">@Model.FechaRegistro.ToString("dd/MM/yyyy HH:mm")</span>
                            </div>
                            <div class="col-6 mb-3">
                                <small class="text-muted d-block">Estado</small>
                                <span class="badge bg-warning text-dark">Pendiente</span>
                            </div>
                            <div class="col-12">
                                <small class="text-muted d-block">Empresa</small>
                                <span class="fw-medium">@Model.NombreEmpresa</span>
                            </div>
                        </div>
                    </div>

                    <p class="text-muted small mb-4">
                        <i class="fas fa-info-circle me-1"></i>
                        El proceso de revision suele tomar entre 24-48 horas habiles. Te notificaremos por correo cuando tu cuenta sea aprobada.
                    </p>

                    <a href="/Feed" class="btn btn-outline-primary">
                        <i class="fas fa-arrow-left me-2"></i>Volver al Feed
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
