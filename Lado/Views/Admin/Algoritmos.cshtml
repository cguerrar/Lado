@model IEnumerable<Lado.Models.AlgoritmoFeed>
@{
    ViewData["Title"] = "Algoritmos de Feed";
    var usosPorAlgoritmo = ViewBag.UsosPorAlgoritmo as Dictionary<int, int> ?? new Dictionary<int, int>();
    var totalUsuariosConPreferencia = ViewBag.TotalUsuariosConPreferencia ?? 0;
}

<style>
    .algoritmos-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 24px;
    }

    .algoritmos-header h2 {
        font-size: 1.5rem;
        font-weight: 700;
        margin: 0;
    }

    .stats-overview {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 20px;
        margin-bottom: 32px;
    }

    .stat-mini {
        background: var(--bg-white);
        border: 1px solid var(--border-color);
        border-radius: var(--radius-lg);
        padding: 20px;
        display: flex;
        align-items: center;
        gap: 16px;
    }

    .stat-mini-icon {
        width: 48px;
        height: 48px;
        border-radius: var(--radius-md);
        display: flex;
        align-items: center;
        justify-content: center;
        background: var(--primary-gradient);
        color: white;
    }

    .stat-mini-icon svg {
        width: 24px;
        height: 24px;
    }

    .stat-mini-content h4 {
        font-size: 1.5rem;
        font-weight: 700;
        margin: 0;
        color: var(--text-dark);
    }

    .stat-mini-content p {
        font-size: 0.8125rem;
        color: var(--text-gray);
        margin: 4px 0 0;
    }

    /* Algoritmos Grid */
    .algoritmos-grid {
        display: grid;
        gap: 20px;
    }

    .algoritmo-card {
        background: var(--bg-white);
        border: 1px solid var(--border-color);
        border-radius: var(--radius-lg);
        padding: 24px;
        transition: all var(--transition-fast);
        position: relative;
    }

    .algoritmo-card:hover {
        box-shadow: var(--shadow-md);
    }

    .algoritmo-card.inactive {
        opacity: 0.6;
        background: var(--bg-light);
    }

    .algoritmo-card.default {
        border-color: var(--primary);
        border-width: 2px;
    }

    .algoritmo-header {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        margin-bottom: 16px;
    }

    .algoritmo-info {
        display: flex;
        align-items: center;
        gap: 16px;
    }

    .algoritmo-icon {
        width: 56px;
        height: 56px;
        border-radius: var(--radius-md);
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
    }

    .algoritmo-icon.cronologico {
        background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
    }

    .algoritmo-icon.trending {
        background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
    }

    .algoritmo-icon.seguidos {
        background: linear-gradient(135deg, #10b981 0%, #059669 100%);
    }

    .algoritmo-icon.para_ti {
        background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
    }

    .algoritmo-icon svg {
        width: 28px;
        height: 28px;
        color: white;
    }

    .algoritmo-details h3 {
        font-size: 1.125rem;
        font-weight: 700;
        margin: 0 0 4px;
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .algoritmo-details .codigo {
        font-size: 0.75rem;
        color: var(--text-gray);
        font-family: monospace;
        background: var(--bg-light);
        padding: 2px 8px;
        border-radius: 4px;
    }

    .default-badge {
        background: var(--primary);
        color: white;
        font-size: 0.6875rem;
        font-weight: 600;
        padding: 3px 8px;
        border-radius: 4px;
        text-transform: uppercase;
    }

    .algoritmo-descripcion {
        color: var(--text-gray);
        font-size: 0.875rem;
        line-height: 1.5;
        margin-bottom: 16px;
    }

    .algoritmo-stats {
        display: flex;
        gap: 24px;
        padding: 16px 0;
        border-top: 1px solid var(--border-color);
        border-bottom: 1px solid var(--border-color);
        margin-bottom: 16px;
    }

    .algoritmo-stat {
        text-align: center;
    }

    .algoritmo-stat-value {
        font-size: 1.25rem;
        font-weight: 700;
        color: var(--text-dark);
    }

    .algoritmo-stat-label {
        font-size: 0.75rem;
        color: var(--text-gray);
        margin-top: 2px;
    }

    .algoritmo-actions {
        display: flex;
        gap: 8px;
        flex-wrap: wrap;
    }

    .btn-sm {
        padding: 8px 14px;
        font-size: 0.8125rem;
    }

    .btn-toggle {
        background: var(--bg-light);
        border: 1px solid var(--border-color);
        color: var(--text-dark);
    }

    .btn-toggle:hover {
        background: var(--primary-pale);
        border-color: var(--primary);
    }

    .btn-toggle.active {
        background: var(--success);
        border-color: var(--success);
        color: white;
    }

    .btn-default {
        background: var(--primary-gradient);
        border: none;
        color: white;
    }

    .btn-default:hover {
        opacity: 0.9;
    }

    .btn-default:disabled {
        opacity: 0.5;
        cursor: not-allowed;
    }

    /* Info Section */
    .info-section {
        background: var(--primary-pale);
        border: 1px solid var(--primary-light);
        border-radius: var(--radius-lg);
        padding: 20px;
        margin-top: 32px;
    }

    .info-section h4 {
        font-size: 1rem;
        font-weight: 600;
        margin: 0 0 12px;
        color: var(--primary-dark);
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .info-section h4 svg {
        width: 20px;
        height: 20px;
    }

    .info-section p {
        font-size: 0.875rem;
        color: var(--text-gray);
        margin: 0;
        line-height: 1.6;
    }

    .info-list {
        list-style: none;
        padding: 0;
        margin: 12px 0 0;
    }

    .info-list li {
        font-size: 0.875rem;
        color: var(--text-gray);
        padding: 6px 0;
        display: flex;
        align-items: flex-start;
        gap: 8px;
    }

    .info-list li svg {
        width: 16px;
        height: 16px;
        color: var(--primary);
        flex-shrink: 0;
        margin-top: 2px;
    }

    @@media (max-width: 768px) {
        .algoritmo-header {
            flex-direction: column;
            gap: 12px;
        }

        .algoritmo-actions {
            width: 100%;
        }

        .algoritmo-actions form,
        .algoritmo-actions button {
            flex: 1;
        }
    }
</style>

<div class="algoritmos-header">
    <h2>Algoritmos de Feed</h2>
    <form asp-action="RepararAlgoritmos" method="post" style="display:inline;">
        @Html.AntiForgeryToken()
        <button type="submit" class="btn btn-sm" style="background: var(--warning); color: white; border: none; padding: 8px 16px; border-radius: 6px; cursor: pointer;">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width:16px;height:16px;margin-right:6px;vertical-align:middle;">
                <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"></path>
            </svg>
            Reparar Algoritmos
        </button>
    </form>
</div>

<!-- Stats Overview -->
<div class="stats-overview">
    <div class="stat-mini">
        <div class="stat-mini-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M22 12h-4l-3 9L9 3l-3 9H2"></path>
            </svg>
        </div>
        <div class="stat-mini-content">
            <h4>@Model.Count()</h4>
            <p>Algoritmos configurados</p>
        </div>
    </div>
    <div class="stat-mini">
        <div class="stat-mini-icon" style="background: linear-gradient(135deg, #10b981 0%, #059669 100%);">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
                <polyline points="22 4 12 14.01 9 11.01"></polyline>
            </svg>
        </div>
        <div class="stat-mini-content">
            <h4>@Model.Count(a => a.Activo)</h4>
            <p>Algoritmos activos</p>
        </div>
    </div>
    <div class="stat-mini">
        <div class="stat-mini-icon" style="background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                <circle cx="9" cy="7" r="4"></circle>
                <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
                <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
            </svg>
        </div>
        <div class="stat-mini-content">
            <h4>@totalUsuariosConPreferencia</h4>
            <p>Usuarios con preferencia</p>
        </div>
    </div>
    <div class="stat-mini">
        <div class="stat-mini-icon" style="background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="18" y1="20" x2="18" y2="10"></line>
                <line x1="12" y1="20" x2="12" y2="4"></line>
                <line x1="6" y1="20" x2="6" y2="14"></line>
            </svg>
        </div>
        <div class="stat-mini-content">
            <h4>@Model.Sum(a => a.TotalUsos).ToString("N0")</h4>
            <p>Total de usos</p>
        </div>
    </div>
</div>

<!-- Algoritmos Grid -->
<div class="algoritmos-grid">
    @foreach (var algoritmo in Model)
    {
        var usosRecientes = usosPorAlgoritmo.GetValueOrDefault(algoritmo.Id, 0);

        <div class="algoritmo-card @(algoritmo.Activo ? "" : "inactive") @(algoritmo.EsPorDefecto ? "default" : "")">
            <div class="algoritmo-header">
                <div class="algoritmo-info">
                    <div class="algoritmo-icon @algoritmo.Codigo">
                        @switch (algoritmo.Codigo)
                        {
                            case "cronologico":
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <circle cx="12" cy="12" r="10"></circle>
                                    <polyline points="12 6 12 12 16 14"></polyline>
                                </svg>
                                break;
                            case "trending":
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18"></polyline>
                                    <polyline points="17 6 23 6 23 12"></polyline>
                                </svg>
                                break;
                            case "seguidos":
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                                    <circle cx="9" cy="7" r="4"></circle>
                                    <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
                                    <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
                                </svg>
                                break;
                            case "para_ti":
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
                                </svg>
                                break;
                            default:
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <circle cx="12" cy="12" r="10"></circle>
                                    <path d="M12 16v-4"></path>
                                    <path d="M12 8h.01"></path>
                                </svg>
                                break;
                        }
                    </div>
                    <div class="algoritmo-details">
                        <h3>
                            @algoritmo.Nombre
                            @if (algoritmo.EsPorDefecto)
                            {
                                <span class="default-badge">Por defecto</span>
                            }
                        </h3>
                        <span class="codigo">@algoritmo.Codigo</span>
                    </div>
                </div>
            </div>

            <p class="algoritmo-descripcion">
                @(algoritmo.Descripcion ?? "Sin descripcion configurada")
            </p>

            <div class="algoritmo-stats">
                <div class="algoritmo-stat">
                    <div class="algoritmo-stat-value">@algoritmo.TotalUsos.ToString("N0")</div>
                    <div class="algoritmo-stat-label">Usos totales</div>
                </div>
                <div class="algoritmo-stat">
                    <div class="algoritmo-stat-value">@usosRecientes</div>
                    <div class="algoritmo-stat-label">Usuarios (7 dias)</div>
                </div>
                <div class="algoritmo-stat">
                    <div class="algoritmo-stat-value">@algoritmo.Orden</div>
                    <div class="algoritmo-stat-label">Orden</div>
                </div>
            </div>

            <div class="algoritmo-actions">
                <form asp-action="ToggleAlgoritmo" asp-route-id="@algoritmo.Id" method="post" style="display:inline;">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn-sm btn-toggle @(algoritmo.Activo ? "active" : "")">
                        @if (algoritmo.Activo)
                        {
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width:16px;height:16px;margin-right:4px;">
                                <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
                                <polyline points="22 4 12 14.01 9 11.01"></polyline>
                            </svg>
                            <text>Activo</text>
                        }
                        else
                        {
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width:16px;height:16px;margin-right:4px;">
                                <circle cx="12" cy="12" r="10"></circle>
                                <line x1="15" y1="9" x2="9" y2="15"></line>
                                <line x1="9" y1="9" x2="15" y2="15"></line>
                            </svg>
                            <text>Inactivo</text>
                        }
                    </button>
                </form>

                @if (!algoritmo.EsPorDefecto && algoritmo.Activo)
                {
                    <form asp-action="SetAlgoritmoPorDefecto" asp-route-id="@algoritmo.Id" method="post" style="display:inline;">
                        @Html.AntiForgeryToken()
                        <button type="submit" class="btn btn-sm btn-default">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width:16px;height:16px;margin-right:4px;">
                                <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon>
                            </svg>
                            Hacer predeterminado
                        </button>
                    </form>
                }
            </div>
        </div>
    }
</div>

@if (!Model.Any())
{
    <div class="card" style="padding: 60px 20px; text-align: center;">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" style="width:64px;height:64px;margin:0 auto 20px;color:var(--text-light);">
            <path d="M22 12h-4l-3 9L9 3l-3 9H2"></path>
        </svg>
        <h3 style="margin:0 0 8px;color:var(--text-dark);">No hay algoritmos configurados</h3>
        <p style="color:var(--text-gray);margin:0;">Los algoritmos se crearan automaticamente con la migracion.</p>
    </div>
}

<!-- Info Section -->
<div class="info-section">
    <h4>
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="10"></circle>
            <path d="M12 16v-4"></path>
            <path d="M12 8h.01"></path>
        </svg>
        Como funcionan los algoritmos
    </h4>
    <p>Los usuarios pueden seleccionar su algoritmo preferido desde el Feed. El algoritmo por defecto se usa para usuarios nuevos o que no han seleccionado preferencia.</p>
    <ul class="info-list">
        <li>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="10"></circle>
                <polyline points="12 6 12 12 16 14"></polyline>
            </svg>
            <strong>Cronologico:</strong> Ordena posts por fecha de publicacion (mas recientes primero)
        </li>
        <li>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polyline points="23 6 13.5 15.5 8.5 10.5 1 18"></polyline>
                <polyline points="17 6 23 6 23 12"></polyline>
            </svg>
            <strong>Trending:</strong> Prioriza contenido con alto engagement reciente (likes, comentarios, vistas)
        </li>
        <li>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                <circle cx="9" cy="7" r="4"></circle>
            </svg>
            <strong>Seguidos Primero:</strong> 70% contenido de creadores seguidos, 30% descubrimiento
        </li>
        <li>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
            </svg>
            <strong>Para Ti:</strong> Personalizado basado en historial de interacciones y preferencias del usuario
        </li>
    </ul>
</div>
